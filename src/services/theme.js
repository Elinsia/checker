import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
  palette: {
    type: 'dark',
    primary: {
      main: '#BB86FC'
    },
    secondary: {
      main: '#03DAC6',
    },
  }
});

export default theme;